spring:
  datasource:
    username: user
    password: pass
    driver-class-name: org.h2.Driver
    url: jdbc:h2:file:./db/filmorate
  sql:
    init:
      mode: always
  h2:
    console:
      enabled: true